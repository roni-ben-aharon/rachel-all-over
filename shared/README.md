# Shared

Shared TypeScript code belongs here.

Use this folder for domain types, constants, and validation that both `client` and `server` need. Keep it platform-neutral: no React, browser APIs, Firebase client SDK, or server-only imports.
