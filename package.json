{
  "name": "rachel-all-over",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "npm --prefix client run dev",
    "dev:local": "npm --prefix client run dev:local",
    "emulator": "firebase emulators:start --only firestore,auth",
    "seed": "node server/scripts/seed.mjs",
    "build": "npm --prefix client run build",
    "lint": "npm --prefix client run lint",
    "test": "npm --prefix client run test",
    "test:watch": "npm --prefix client run test:watch",
    "e2e": "npm --prefix client run e2e",
    "e2e:ui": "npm --prefix client run e2e:ui"
  }
}
